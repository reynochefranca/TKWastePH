<%= form_for(@request_history) do |f| %>
  <% if @request_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_history.errors.count, "error") %> prohibited this request_history from being saved:</h2>

      <ul>
      <% @request_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requested_at %><br>
    <%= f.datetime_select :requested_at %>
  </div>
  <div class="field">
    <%= f.label :csv_file_path %><br>
    <%= f.text_field :csv_file_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
